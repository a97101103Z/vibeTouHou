services:
  api:
    build:
      context: .
      dockerfile: server/Dockerfile
    environment:
      CORS_ORIGINS: "http://localhost,http://127.0.0.1,http://localhost:80,http://127.0.0.1:80"
      MAX_RENDER_WORKERS: "3"
      MAX_RENDER_QUEUE: "48"
      # Absolute host path to ./data so sandbox containers (spawned as
      # Docker siblings via /var/run/docker.sock) can bind-mount it.
      # Override with an explicit path if ${PWD} doesn't resolve correctly.
      HOST_DATA_DIR: "${PWD}/data"
    volumes:
      - ./data:/app/data
      - ${XDG_RUNTIME_DIR}/docker.sock:/var/run/docker.sock
    expose:
      - "8000"
    restart: unless-stopped

  nginx:
    image: nginx:1.27-alpine
    depends_on:
      - api
    ports:
      - "12345:80"
    volumes:
      - ./deploy/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
